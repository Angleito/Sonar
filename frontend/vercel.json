{
  "framework": "nextjs",
  "installCommand": "cd .. && bun install",
  "buildCommand": "cd .. && bun run build:packages && cd frontend && bun run build",
  "devCommand": "bun run dev",
  "outputDirectory": ".next",
  "functions": {
    "app/api/edge/verify/worker/route.ts": {
      "maxDuration": 300
    }
  }
}
