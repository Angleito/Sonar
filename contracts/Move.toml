[package]
name = "sonar"
version = "0.1.0"
edition = "2024.beta"
license = "Apache-2.0"
authors = ["SONAR Protocol <team@sonar.xyz>"]

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "testnet-v1.43.0" }
Walrus = { local = "./dependencies/walrus" }
WAL = { local = "./dependencies/wal" }

[addresses]
sonar = "0x0"
walrus = "0x0"
# Note: walrus address will be replaced with deployed WAL package ID
# Testnet: 0x261b2e46428a152570f9ac08972d67f7c12d62469ccd381a51774c1df7a829ca

[dev-dependencies]
# Add any test-specific dependencies here if needed
