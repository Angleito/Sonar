{
  "name": "audio-verifier-tests",
  "version": "1.0.0",
  "description": "API integration tests for SONAR audio-verifier service",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:health": "bun test tests/health.test.ts",
    "test:verify": "bun test tests/verify.test.ts",
    "test:auth": "bun test tests/authentication.test.ts",
    "test:e2e": "bun test tests/e2e/*.test.ts",
    "test:ci": "bun test --coverage --reporter json > test-results.json"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "latest",
    "zod": "^3.22.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
